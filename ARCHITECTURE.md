# 🏗️ AI Story Creator - System Architecture

## 📐 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           CLIENT BROWSER                                 │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │                    React Frontend (Port 5173)                     │  │
│  │                                                                   │  │
│  │  ┌─────────────┐  ┌──────────────┐  ┌────────────────────────┐  │  │
│  │  │   Image     │  │     Text     │  │   Voice Recording      │  │  │
│  │  │   Upload    │  │    Input     │  │   (Browser API)        │  │  │
│  │  └──────┬──────┘  └──────┬───────┘  └──────────┬─────────────┘  │  │
│  │         │                │                      │                │  │
│  │         └────────────────┴──────────────────────┘                │  │
│  │                          │                                        │  │
│  │                    ┌─────▼─────┐                                 │  │
│  │                    │   Axios   │                                 │  │
│  │                    │ HTTP Client│                                │  │
│  │                    └─────┬─────┘                                 │  │
│  └──────────────────────────┼─────────────────────────────────────┬─┘  │
└────────────────────────────┼─────────────────────────────────────┼────┘
                             │                                     │
                      HTTP/JSON                            Audio/MP3
                             │                                     │
┌────────────────────────────▼─────────────────────────────────────▼────┐
│                    FastAPI Backend (Port 8000)                         │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │                      API Layer (main.py)                         │ │
│  │  ┌────────────┐  ┌────────────┐  ┌──────────┐  ┌─────────────┐ │ │
│  │  │  /story-   │  │  /story-   │  │  /tts    │  │  /health    │ │ │
│  │  │   from-    │  │   from-    │  │          │  │             │ │ │
│  │  │   image    │  │   text     │  │          │  │             │ │ │
│  │  └─────┬──────┘  └─────┬──────┘  └────┬─────┘  └─────────────┘ │ │
│  └────────┼───────────────┼──────────────┼────────────────────────┘ │
│           │               │              │                           │
│  ┌────────▼───────────────▼──────────────▼────────────────────────┐ │
│  │                   Service Layer                                 │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐ │ │
│  │  │ImageCaptioner│  │StoryGenerator│  │  TextToSpeech        │ │ │
│  │  │              │  │              │  │                      │ │ │
│  │  │  (BLIP)      │  │  (Gemini)    │  │   (gTTS)             │ │ │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────────────┘ │ │
│  └─────────┼──────────────────┼──────────────────┼─────────────────┘ │
└────────────┼──────────────────┼──────────────────┼───────────────────┘
             │                  │                  │
┌────────────▼──────────────────▼──────────────────▼───────────────────┐
│                        External AI Services                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐   │
│  │ HuggingFace  │  │   Google     │  │    Google TTS API        │   │
│  │    Models    │  │  Gemini API  │  │                          │   │
│  │   (BLIP)     │  │   (2.0 Flash)│  │   Multi-language Audio   │   │
│  └──────────────┘  └──────────────┘  └──────────────────────────┘   │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Detailed Component Architecture

### **Frontend Layer (React + TypeScript)**

```
┌─────────────────────────────────────────────────────────┐
│                    src/main.tsx                         │
│                  (Entry Point)                          │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│                     src/App.tsx                         │
│              (Root Component + Routing)                 │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│                 src/pages/Home.tsx                      │
│            (Main Application Logic)                     │
│                                                         │
│  State Management (useState):                           │
│  • activeTab (image/text/voice)                         │
│  • selectedImage, textPrompt, audioBlob                 │
│  • selectedLanguage (9 options)                         │
│  • selectedAgeCategory (3 options)                      │
│  • isGenerating (loading state)                         │
│  • storyResult (API response)                           │
│                                                         │
│  Event Handlers:                                        │
│  • handleImageUpload()                                  │
│  • handleTextSubmit()                                   │
│  • startRecording() / stopRecording()                   │
│  • generateStory()                                      │
└────────────────────────┬────────────────────────────────┘
                         │
          ┌──────────────┼──────────────┐
          │              │              │
          ▼              ▼              ▼
┌─────────────────┐ ┌──────────┐ ┌───────────────┐
│  ImageUploader  │ │ Language │ │ AgeCategory   │
│                 │ │ Selector │ │   Selector    │
└─────────────────┘ └──────────┘ └───────────────┘
          │              │              │
          └──────────────┼──────────────┘
                         │
          ┌──────────────┴──────────────┐
          ▼                             ▼
┌─────────────────┐           ┌─────────────────┐
│  StoryDisplay   │           │  AudioPlayer    │
│  • Story text   │           │  • MP3 playback │
│  • Caption      │           │  • Controls     │
│  • Formatting   │           │  • Download     │
└─────────────────┘           └─────────────────┘
```

---

### **Backend Layer (FastAPI + Python)**

```
┌──────────────────────────────────────────────────────────────┐
│                      main.py (FastAPI App)                   │
│                                                              │
│  Middleware:                                                 │
│  • CORS (Cross-Origin Resource Sharing)                      │
│  • Static Files (/audio directory)                           │
│                                                              │
│  Routes:                                                     │
│  • POST /story-from-image ──────────────────┐               │
│  • POST /story-from-text ────────────────┐  │               │
│  • POST /caption ──────────────────┐     │  │               │
│  • POST /translate ─────────┐      │     │  │               │
│  • POST /tts ─────────┐     │      │     │  │               │
│  • GET  /health       │     │      │     │  │               │
└───────────────────────┼─────┼──────┼─────┼──┼───────────────┘
                        │     │      │     │  │
                        ▼     ▼      ▼     ▼  ▼
┌──────────────────────────────────────────────────────────────┐
│                      Service Layer                           │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │  image_caption.py (ImageCaptioner)                 │     │
│  │                                                    │     │
│  │  class ImageCaptioner:                             │     │
│  │    • __init__(): Load BLIP model                   │     │
│  │    • generate_caption(image_path)                  │     │
│  │      ├─ Preprocess image                           │     │
│  │      ├─ Run BLIP inference                         │     │
│  │      └─ Return caption string                      │     │
│  │                                                    │     │
│  │  Model: Salesforce/blip-image-captioning-large     │     │
│  │  Device: CPU (optimized)                           │     │
│  │  Cache: ~/.cache/huggingface/                      │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │  story_gen.py (StoryGenerator)                     │     │
│  │                                                    │     │
│  │  class StoryGenerator:                             │     │
│  │    • __init__(): Initialize Gemini API             │     │
│  │    • enhance_caption(caption)                      │     │
│  │      └─ Add descriptive keywords                   │     │
│  │    • generate_story(caption, language, age)        │     │
│  │      ├─ Create age-appropriate prompt              │     │
│  │      ├─ Call Gemini API (multi-turn)               │     │
│  │      └─ Return enhanced story                      │     │
│  │                                                    │     │
│  │  API: Google Gemini 2.0 Flash                      │     │
│  │  Features: Context-aware, age filtering            │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │  translate.py (Translator)                         │     │
│  │                                                    │     │
│  │  class Translator:                                 │     │
│  │    • translate(text, target_lang)                  │     │
│  │      ├─ Detect source language                     │     │
│  │      ├─ Call Google Translate API                  │     │
│  │      └─ Return translated text                     │     │
│  │                                                    │     │
│  │  Supported: 9 languages (en, hi, ta, te, etc.)     │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │  tts.py (TextToSpeech)                             │     │
│  │                                                    │     │
│  │  class TextToSpeech:                               │     │
│  │    • generate_audio(text, language)                │     │
│  │      ├─ Create gTTS object                         │     │
│  │      ├─ Generate MP3 file                          │     │
│  │      ├─ Save to audio/ directory                   │     │
│  │      └─ Return audio URL                           │     │
│  │                                                    │     │
│  │  Output: MP3 format, multi-language                │     │
│  └────────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Architecture

### **1. Image-to-Story Complete Flow**

```
┌─────────────┐
│   User      │
│  Uploads    │
│   Image     │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│  Frontend: ImageUploader Component                   │
│  • File validation (JPG, PNG)                        │
│  • Preview display                                   │
│  • Convert to FormData                               │
└──────┬───────────────────────────────────────────────┘
       │
       │ HTTP POST /story-from-image
       │ Content-Type: multipart/form-data
       │ {image: File, language: string, age_category: string}
       ▼
┌──────────────────────────────────────────────────────┐
│  Backend: main.py - /story-from-image endpoint       │
│  1. Save uploaded image to uploads/                  │
│  2. Call ImageCaptioner.generate_caption()           │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│  image_caption.py                                    │
│  • Load image with PIL                               │
│  • Preprocess for BLIP (resize, normalize)           │
│  • Run BLIP model inference                          │
│  • Extract caption text                              │
│  • Return: "a girl with a fox in a room"             │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│  Backend: Enhance caption                            │
│  • Add descriptive keywords                          │
│  • Return: Enhanced caption                          │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│  story_gen.py                                        │
│  • Create age-appropriate prompt                     │
│  • Call Gemini API (Turn 1)                          │
│    Prompt: "Create a {age} story about {caption}"    │
│  • Get initial story (~300 words)                    │
│  • Call Gemini API (Turn 2)                          │
│    Prompt: "Expand with more details"                │
│  • Merge both responses                              │
│  • Return: Complete story (500-700 words)            │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│  Backend: Translation (if language != 'en')          │
│  • translate.py: Convert to target language          │
│  • Preserve formatting and structure                 │
└──────┬───────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────┐
│  tts.py                                              │
│  • Create gTTS object with language                  │
│  • Generate speech from story text                   │
│  • Save as MP3: audio/{uuid}.mp3                     │
│  • Return audio URL                                  │
└──────┬───────────────────────────────────────────────┘
       │
       │ HTTP 200 Response
       │ {story: string, caption: string, audio_url: string}
       ▼
┌──────────────────────────────────────────────────────┐
│  Frontend: Home.tsx                                  │
│  • Update storyResult state                          │
│  • Render StoryDisplay component                     │
│  • Render AudioPlayer component                      │
│  • Show success toast notification                   │
└──────────────────────────────────────────────────────┘
```

---

### **2. Text-to-Story Flow**

```
User Input → Text Prompt → StoryGenerator.generate_story()
           ↓
       Gemini API (2 turns) → Translation (if needed)
           ↓
       TTS Generation → Return {story, audio_url}
```

### **3. Voice-to-Story Flow**

```
Microphone → Browser MediaRecorder API → Audio Blob
                       ↓
            Browser Speech-to-Text → Text String
                       ↓
            [Same as Text-to-Story Flow]
```

---

## 🗄️ Directory Structure & Responsibilities

```
Deep_learning/
│
├── frontend/                          # React Application
│   ├── src/
│   │   ├── main.tsx                  # Entry point
│   │   ├── App.tsx                   # Root component
│   │   ├── index.css                 # Global styles + animations
│   │   │
│   │   ├── components/               # Reusable UI Components
│   │   │   ├── ImageUploader.tsx     # Image drag-drop + preview
│   │   │   ├── LanguageSelector.tsx  # 9 language dropdown
│   │   │   ├── AgeCategorySelector.tsx # Age group selection
│   │   │   ├── StoryDisplay.tsx      # Story text rendering
│   │   │   └── AudioPlayer.tsx       # MP3 player controls
│   │   │
│   │   └── pages/
│   │       └── Home.tsx              # Main app logic + state
│   │
│   ├── public/                       # Static assets
│   ├── package.json                  # Dependencies
│   ├── tsconfig.json                 # TypeScript config
│   ├── vite.config.ts               # Vite build config
│   ├── tailwind.config.js           # Tailwind CSS config
│   └── postcss.config.js            # PostCSS config
│
├── backend/                          # FastAPI Application
│   ├── main.py                      # FastAPI app + routes
│   │   ├── CORS middleware
│   │   ├── Static file serving
│   │   └── API endpoints
│   │
│   ├── image_caption.py             # BLIP image captioning
│   │   └── ImageCaptioner class
│   │
│   ├── story_gen.py                 # Gemini story generation
│   │   └── StoryGenerator class
│   │
│   ├── translate.py                 # Google Translate wrapper
│   │   └── Translator class
│   │
│   ├── tts.py                       # Text-to-speech
│   │   └── TextToSpeech class
│   │
│   ├── speech_to_text.py            # (Future: Backend STT)
│   ├── video_generation.py          # (Inactive)
│   │
│   ├── .env                         # Environment variables
│   │   └── GEMINI_API_KEY
│   │
│   ├── requirements.txt             # Python dependencies
│   │
│   ├── models/                      # Cached AI models
│   │   └── (HuggingFace cache)
│   │
│   ├── uploads/                     # Temporary image storage
│   │   └── {uuid}_{filename}.jpg
│   │
│   └── audio/                       # Generated MP3 files
│       └── {uuid}.mp3
│
├── README.md                        # Project documentation
├── FEATURES_AND_TECH.md            # This file
└── ARCHITECTURE.md                  # Architecture documentation
```

---

## 🔐 Security Architecture

```
┌──────────────────────────────────────────────────────┐
│               Security Layers                        │
├──────────────────────────────────────────────────────┤
│                                                      │
│  1. Frontend Validation                              │
│     • File type checking (image/*)                   │
│     • File size limits                               │
│     • Input sanitization                             │
│                                                      │
│  2. CORS Protection                                  │
│     • Allowed origins: localhost:5173, 5174          │
│     • Credentials: true                              │
│     • Methods: POST, GET, OPTIONS                    │
│                                                      │
│  3. Backend Validation                               │
│     • Pydantic models (type checking)                │
│     • File type validation                           │
│     • Request size limits                            │
│                                                      │
│  4. Environment Variables                            │
│     • .env file (not in git)                         │
│     • API keys protected                             │
│     • No hardcoded secrets                           │
│                                                      │
│  5. File Cleanup                                     │
│     • Temporary files deleted after processing       │
│     • No persistent user data                        │
│                                                      │
│  6. API Rate Limiting (Future)                       │
│     • Per-IP request limits                          │
│     • API key rotation                               │
└──────────────────────────────────────────────────────┘
```

---

## ⚡ Performance Architecture

### **Optimization Strategies**

```
┌──────────────────────────────────────────────────────┐
│           Performance Optimizations                  │
├──────────────────────────────────────────────────────┤
│                                                      │
│  Frontend:                                           │
│  ✅ Vite HMR (Hot Module Replacement)                │
│  ✅ Code splitting (lazy loading)                    │
│  ✅ Asset optimization (images, fonts)               │
│  ✅ React.memo for heavy components                  │
│  ✅ Debounced inputs                                 │
│  ✅ Optimistic UI updates                            │
│                                                      │
│  Backend:                                            │
│  ✅ Model caching (load once)                        │
│  ✅ Async operations (asyncio)                       │
│  ✅ Connection pooling                               │
│  ✅ Static file CDN (audio)                          │
│  ✅ Compression (gzip)                               │
│                                                      │
│  AI Models:                                          │
│  ✅ CPU-optimized BLIP                               │
│  ✅ Cached model weights                             │
│  ✅ Batched processing (future)                      │
│  ✅ GPU support (when available)                     │
└──────────────────────────────────────────────────────┘
```

### **Caching Strategy**

```
┌─────────────────────────────────────────┐
│  Cache Layer 1: Browser (Frontend)     │
│  • Static assets (Vite)                 │
│  • Audio files (browser cache)          │
│  • Component state (React)              │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│  Cache Layer 2: Backend Memory          │
│  • Loaded AI models (PyTorch)           │
│  • API clients (Gemini, gTTS)           │
│  • Translation cache (optional)         │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│  Cache Layer 3: Disk                    │
│  • HuggingFace model cache              │
│  • Generated audio files (temp)         │
│  • Uploaded images (temp)               │
└─────────────────────────────────────────┘
```

---

## 🔌 API Architecture

### **RESTful API Design**

```
Base URL: http://localhost:8000

┌──────────────────────────────────────────────────────┐
│  Endpoint: POST /story-from-image                    │
├──────────────────────────────────────────────────────┤
│  Request:                                            │
│    Content-Type: multipart/form-data                 │
│    Body:                                             │
│      • image: File (required)                        │
│      • language: string (default: "en")              │
│      • age_category: string (default: "5-12")        │
│                                                      │
│  Response:                                           │
│    Status: 200 OK                                    │
│    Body: {                                           │
│      story: string (500-700 words),                  │
│      caption: string (BLIP output),                  │
│      audio_url: string (MP3 URL)                     │
│    }                                                 │
│                                                      │
│  Processing Time: ~5-10 seconds                      │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│  Endpoint: POST /story-from-text                     │
├──────────────────────────────────────────────────────┤
│  Request:                                            │
│    Content-Type: application/json                    │
│    Body: {                                           │
│      text: string (required),                        │
│      language: string (default: "en"),               │
│      age_category: string (default: "5-12")          │
│    }                                                 │
│                                                      │
│  Response:                                           │
│    Status: 200 OK                                    │
│    Body: {                                           │
│      story: string,                                  │
│      audio_url: string                               │
│    }                                                 │
│                                                      │
│  Processing Time: ~3-5 seconds                       │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│  Endpoint: POST /tts                                 │
├──────────────────────────────────────────────────────┤
│  Request:                                            │
│    Body: {                                           │
│      text: string (required),                        │
│      language: string (default: "en")                │
│    }                                                 │
│                                                      │
│  Response:                                           │
│    Body: {                                           │
│      audio_url: string                               │
│    }                                                 │
│                                                      │
│  Processing Time: ~1-2 seconds                       │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│  Endpoint: GET /health                               │
├──────────────────────────────────────────────────────┤
│  Response:                                           │
│    Body: {                                           │
│      status: "healthy",                              │
│      models_loaded: {                                │
│        captioner: boolean,                           │
│        story_generator: boolean,                     │
│        translator: boolean,                          │
│        tts: boolean                                  │
│      }                                               │
│    }                                                 │
└──────────────────────────────────────────────────────┘
```

---

## 🌐 Deployment Architecture

### **Development Environment**
```
Frontend (Vite Dev Server)    Backend (Uvicorn Dev Server)
http://localhost:5173    ←─────→  http://localhost:8000
         ↓                              ↓
   React Hot Reload              Auto-reload on file changes
```

### **Production Environment (Recommended)**
```
┌─────────────────────────────────────────────────────┐
│                  Load Balancer / Nginx              │
│              (SSL Termination, Reverse Proxy)       │
└─────────────┬───────────────────────┬───────────────┘
              │                       │
     ┌────────▼────────┐    ┌─────────▼──────────┐
     │   Frontend      │    │   Backend          │
     │   (Static)      │    │   (Gunicorn +      │
     │   Nginx/CDN     │    │    FastAPI)        │
     │   Port 80/443   │    │   Port 8000        │
     └─────────────────┘    └─────────┬──────────┘
                                      │
                            ┌─────────▼──────────┐
                            │   AI Models        │
                            │   (Cached)         │
                            └────────────────────┘
```

---

## 📊 State Management Architecture

### **Frontend State Flow**
```
┌──────────────────────────────────────────────────────┐
│                 Home.tsx State                       │
├──────────────────────────────────────────────────────┤
│                                                      │
│  Input State:                                        │
│  • activeTab: 'image' | 'text' | 'voice'             │
│  • selectedImage: File | null                        │
│  • textPrompt: string                                │
│  • audioBlob: Blob | null                            │
│  • recordingTime: number                             │
│                                                      │
│  Configuration State:                                │
│  • selectedLanguage: string (9 options)              │
│  • selectedAgeCategory: string (3 options)           │
│                                                      │
│  Processing State:                                   │
│  • isGenerating: boolean                             │
│  • isRecording: boolean                              │
│  • currentStep: string                               │
│                                                      │
│  Result State:                                       │
│  • storyResult: {                                    │
│      story: string,                                  │
│      caption: string,                                │
│      audio_url: string                               │
│    } | null                                          │
│                                                      │
│  Refs:                                               │
│  • mediaRecorderRef: MediaRecorder | null            │
│  • timerRef: number | null                           │
└──────────────────────────────────────────────────────┘
```

---

## 🎯 Technology Decision Matrix

| Component | Technology | Reason |
|-----------|-----------|--------|
| **Frontend Framework** | React 18 | Modern, component-based, large ecosystem |
| **Type Safety** | TypeScript | Catch errors at compile-time, better DX |
| **Build Tool** | Vite | Fast HMR, modern ESM, optimized builds |
| **Styling** | Tailwind CSS | Utility-first, responsive, customizable |
| **State Management** | React Hooks | Built-in, no extra dependencies |
| **HTTP Client** | Axios | Promise-based, interceptors, cancellation |
| **Backend Framework** | FastAPI | Fast, async, auto-documentation |
| **Server** | Uvicorn | ASGI, high performance |
| **AI Models** | BLIP + Gemini | State-of-art, pre-trained, accessible |
| **Image Processing** | Pillow | Standard Python library |
| **Audio Generation** | gTTS | Free, multi-language, reliable |

---

**Architecture Version**: 1.0.0  
**Last Updated**: October 28, 2025  
**Status**: Production Ready 🎉
